/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Salud Nexus · API del centro sanitario
 * API REST versionada para el portal de pacientes de una instalación de Salud Nexus. El despliegue representa un solo centro; los identificadores internos de organización y centro se conservan únicamente para aislamiento, integridad referencial y auditoría. Los datos clínicos requieren una sesión de navegador Laravel Sanctum y todas las mutaciones están protegidas mediante CSRF. Los errores siguen RFC 9457 y nunca incluyen detalles clínicos o internos.
 * OpenAPI spec version: 1.0.0
 */
import { z as zod } from 'zod';
import { RequestId } from './requestId.zod';

export const problemTypeRegExp = new RegExp('^https://salud-nexus\\.example/problems/[a-z0-9-]+$');
export const problemTitleMax = 200;

export const problemStatusMin = 400;
export const problemStatusMax = 599;

export const problemDetailMax = 2000;

export const problemInstanceRegExp = new RegExp('^/api/v1(?:/.*)?$');

export const Problem = zod.strictObject({
  type: zod.url().regex(problemTypeRegExp),
  title: zod.string().min(1).max(problemTitleMax),
  status: zod.number().min(problemStatusMin).max(problemStatusMax),
  detail: zod.string().min(1).max(problemDetailMax),
  instance: zod.string().regex(problemInstanceRegExp),
  request_id: RequestId,
  errors: zod.record(zod.string(), zod.array(zod.string()).min(1)).optional(),
});

export type Problem = zod.input<typeof Problem>;
export type ProblemOutput = zod.output<typeof Problem>;
